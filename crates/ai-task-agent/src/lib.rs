//! 远端 agent 的线上协议。
//!
//! 单独做成 lib 是为了让中心节点（`ai-task-exec`）和 agent 共用同一份类型定义。
//! 两边各写一份迟早会漂移，而漂移的表现是「远端返回的字段被静默丢弃」。
//!
//! 这个 crate **只有 serde 依赖**：它会被链进要经 SSH 推送的 musl 静态二进制，
//! 每多一个依赖都是目标机上多出来的字节。

pub mod protocol;
